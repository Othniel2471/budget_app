<%= form_with(model: category, html: { class: 'your-form-class' }) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: 'your-text-field-class' %>
  </div>

  <div>
    <%= form.label :icon, style: "display: block" %>
    <%= form.select :icon, options_for_select(@options, selected: @category.icon), { prompt: "Select an icon" }, class: 'your-text-field-class' %>
  </div>

   <div class="create-new-cate">
    <%= form.submit 'Submit', class: 'submit-btn' %>
  </div>
<% end %>
